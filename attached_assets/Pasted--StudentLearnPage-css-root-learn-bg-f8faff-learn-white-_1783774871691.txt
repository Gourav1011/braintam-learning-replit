/* StudentLearnPage.css */

:root {
  --learn-bg:
    #f8faff;

  --learn-white:
    #ffffff;

  --learn-navy:
    #102a56;

  --learn-muted:
    #68758e;

  --learn-border:
    #e3e9f3;

  --learn-green:
    #12ad69;

  --learn-green-soft:
    #eaf9f2;

  --learn-purple:
    #7139e8;

  --learn-purple-soft:
    #f4efff;

  --learn-blue:
    #3d9cf5;

  --learn-orange:
    #ff9e16;

  --learn-pink:
    #ed4fb0;

  --learn-shadow:
    0 8px 25px
    rgba(
      25,
      55,
      95,
      0.07
    );
}

.learn-page {
  min-height:
    100dvh;

  color:
    var(
      --learn-navy
    );

  background:
    var(
      --learn-bg
    );
}

.learn-content {
  width:
    min(
      1240px,
      calc(
        100% - 48px
      )
    );

  margin:
    0 auto;

  padding:
    24px 0 80px;
}

.student-topbar {
  display: none;
}

.learn-header {
  position:
    relative;

  min-height:
    112px;

  display: flex;

  align-items:
    center;

  justify-content:
    space-between;

  padding:
    22px 28px;

  overflow:
    hidden;

  background:
    linear-gradient(
      135deg,
      #ffffff,
      #eef8ff
    );

  border:
    1px solid
    #e5edf6;

  border-radius:
    20px;
}

.learn-header h1 {
  display: flex;

  align-items:
    center;

  gap: 12px;

  margin: 0;

  font-size:
    clamp(
      30px,
      4vw,
      42px
    );

  line-height:
    1.1;

  letter-spacing:
    -0.04em;
}

.learn-header p {
  margin:
    9px 0 0;

  color:
    var(
      --learn-muted
    );

  font-size:
    15px;
}

.learn-header-art {
  position:
    relative;

  width:
    210px;

  height:
    85px;
}

.header-planet {
  position:
    absolute;

  top: -7px;

  left: 15px;

  font-size:
    65px;

  animation:
    headerFloat
    6s
    ease-in-out
    infinite;
}

.header-rocket {
  position:
    absolute;

  right: 15px;

  bottom: 0;

  font-size:
    48px;

  animation:
    rocketFloat
    5s
    ease-in-out
    infinite;
}

.learning-tabs {
  display:
    inline-flex;

  gap: 5px;

  margin-top:
    20px;

  padding:
    5px;

  background:
    #edf1f7;

  border-radius:
    14px;
}

.learning-tab {
  min-height:
    44px;

  display:
    inline-flex;

  align-items:
    center;

  justify-content:
    center;

  gap: 8px;

  padding:
    9px 18px;

  color:
    var(
      --learn-muted
    );

  background:
    transparent;

  border: 0;

  border-radius:
    10px;

  cursor:
    pointer;

  font-weight:
    800;

  transition:
    180ms ease;
}

.learning-tab:hover {
  color:
    var(
      --learn-navy
    );
}

.learning-tab.active {
  color:
    var(
      --learn-green
    );

  background:
    #ffffff;

  box-shadow:
    0 4px 12px
    rgba(
      20,
      50,
      90,
      0.08
    );
}

.tab-count {
  min-width:
    22px;

  height:
    22px;

  display:
    inline-grid;

  place-items:
    center;

  padding:
    0 6px;

  color:
    #ffffff;

  background:
    var(
      --learn-green
    );

  border-radius:
    999px;

  font-size:
    11px;
}

.learn-search-row {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1fr
    )
    auto;

  gap: 12px;

  margin:
    18px 0;
}

.learn-search,
.subject-filter {
  min-height:
    50px;

  display:
    flex;

  align-items:
    center;

  gap: 10px;

  padding:
    0 17px;

  background:
    #ffffff;

  border:
    1px solid
    var(
      --learn-border
    );

  border-radius:
    15px;

  box-shadow:
    0 3px 10px
    rgba(
      30,
      60,
      100,
      0.04
    );
}

.learn-search {
  color:
    #8994a8;
}

.learn-search input {
  width:
    100%;

  color:
    var(
      --learn-navy
    );

  background:
    transparent;

  border: 0;

  outline: 0;

  font-size:
    14px;
}

.subject-filter {
  position:
    relative;

  min-width:
    160px;
}

.subject-filter select {
  width:
    100%;

  padding-right:
    25px;

  color:
    var(
      --learn-navy
    );

  background:
    transparent;

  border: 0;

  outline: 0;

  appearance:
    none;

  font-weight:
    750;
}

.subject-filter svg {
  position:
    absolute;

  right: 14px;

  pointer-events:
    none;
}

.ignite-program {
  position:
    relative;

  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1.5fr
    )
    180px
    minmax(
      240px,
      0.6fr
    );

  gap: 22px;

  align-items:
    center;

  padding:
    24px 28px;

  overflow:
    hidden;

  background:
    linear-gradient(
      135deg,
      #ffffff,
      #effcf8
    );

  border:
    1px solid
    rgba(
      18,
      173,
      105,
      0.25
    );

  border-left:
    4px solid
    var(
      --learn-green
    );

  border-radius:
    18px;

  box-shadow:
    var(
      --learn-shadow
    );
}

.section-label {
  margin:
    0 0 14px;

  font-size:
    12px;

  font-weight:
    900;

  letter-spacing:
    0.025em;

  text-transform:
    uppercase;
}

.section-label.green {
  color:
    #078956;
}

.section-label.purple {
  color:
    var(
      --learn-purple
    );
}

.ignite-title-row {
  display:
    flex;

  align-items:
    center;

  gap: 14px;
}

.ignite-icon {
  font-size:
    38px;
}

.title-with-badge {
  display:
    flex;

  align-items:
    center;

  flex-wrap:
    wrap;

  gap: 10px;
}

.title-with-badge h2 {
  margin: 0;

  font-size:
    clamp(
      21px,
      3vw,
      29px
    );

  letter-spacing:
    -0.03em;
}

.grade-badge {
  padding:
    5px 10px;

  color:
    #2967e8;

  background:
    #eaf1ff;

  border-radius:
    999px;

  font-size:
    12px;

  font-weight:
    850;
}

.ignite-title-row p {
  margin:
    5px 0 0;

  color:
    var(
      --learn-muted
    );
}

.ignite-days {
  display:
    flex;

  gap: 7px;

  margin-top:
    18px;

  overflow-x:
    auto;

  scrollbar-width:
    none;
}

.ignite-days::-webkit-scrollbar {
  display:
    none;
}

.ignite-day {
  min-width:
    72px;

  min-height:
    39px;

  display:
    inline-flex;

  align-items:
    center;

  justify-content:
    center;

  gap: 6px;

  padding:
    8px 13px;

  color:
    #758197;

  background:
    #ffffff;

  border:
    1px solid
    #dde4ed;

  border-radius:
    999px;

  font-size:
    12px;

  font-weight:
    800;
}

.ignite-day.complete {
  color:
    #ffffff;

  background:
    var(
      --learn-green
    );

  border-color:
    var(
      --learn-green
    );
}

.ignite-day.locked {
  opacity:
    0.65;
}

.empty-day {
  width:
    13px;

  height:
    13px;

  border:
    2px solid
    #c8d0dc;

  border-radius:
    50%;
}

.ignite-progress-row {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1fr
    )
    auto;

  align-items:
    center;

  gap: 14px;

  margin-top:
    17px;
}

.progress-track {
  width:
    100%;

  height:
    8px;

  overflow:
    hidden;

  background:
    #e9edf3;

  border-radius:
    999px;
}

.progress-fill {
  height:
    100%;

  border-radius:
    inherit;

  transition:
    width
    800ms
    cubic-bezier(
      0.2,
      0.8,
      0.2,
      1
    );
}

.progress-fill.green {
  background:
    linear-gradient(
      90deg,
      #10a765,
      #43d99c
    );
}

.progress-fill.purple {
  background:
    linear-gradient(
      90deg,
      #7139e8,
      #a976f5
    );
}

.ignite-progress-row strong {
  color:
    #07945a;
}

.ignite-rocket-art {
  display:
    grid;

  place-items:
    center;

  font-size:
    95px;

  transform:
    rotate(
      10deg
    );

  animation:
    rocketFloat
    5s
    ease-in-out
    infinite;
}

.next-session {
  padding:
    20px;

  background:
    rgba(
      255,
      255,
      255,
      0.84
    );

  border:
    1px solid
    #e4eee9;

  border-radius:
    15px;
}

.next-session-title {
  display:
    flex;

  align-items:
    center;

  gap: 9px;

  color:
    #089760;
}

.next-session p {
  min-height:
    42px;

  margin:
    13px 0 18px;

  color:
    var(
      --learn-muted
    );
}

.primary-button {
  min-height:
    43px;

  display:
    inline-flex;

  align-items:
    center;

  justify-content:
    center;

  gap: 7px;

  padding:
    10px 17px;

  color:
    #ffffff;

  text-decoration:
    none;

  border: 0;

  border-radius:
    10px;

  cursor:
    pointer;

  font-size:
    13px;

  font-weight:
    850;

  transition:
    transform
    180ms ease,
    box-shadow
    180ms ease;
}

.primary-button:hover {
  transform:
    translateY(
      -1px
    );
}

.primary-button.green {
  background:
    linear-gradient(
      135deg,
      #11b16c,
      #079b5d
    );

  box-shadow:
    0 7px 17px
    rgba(
      10,
      164,
      96,
      0.2
    );
}

.primary-button.purple {
  background:
    linear-gradient(
      135deg,
      #7d45ee,
      #5e28d4
    );

  box-shadow:
    0 7px 17px
    rgba(
      105,
      48,
      220,
      0.2
    );
}

.learning-main-grid {
  display:
    grid;

  grid-template-columns:
    minmax(
      310px,
      0.7fr
    )
    minmax(
      0,
      1.3fr
    );

  gap: 20px;

  align-items:
    start;

  margin-top:
    20px;
}

.continue-card {
  min-height:
    290px;

  padding:
    24px;

  background:
    linear-gradient(
      135deg,
      #ffffff,
      #f5f0ff
    );

  border:
    1px solid
    #e4d9ff;

  border-radius:
    18px;

  box-shadow:
    var(
      --learn-shadow
    );
}

.continue-content {
  display:
    flex;

  gap: 15px;

  align-items:
    flex-start;
}

.continue-icon,
.continue-empty-icon {
  width:
    58px;

  height:
    58px;

  flex:
    0 0 58px;

  display:
    grid;

  place-items:
    center;

  color:
    #ffffff;

  background:
    linear-gradient(
      135deg,
      #8a55f3,
      #6230d6
    );

  border-radius:
    16px;

  box-shadow:
    0 8px 18px
    rgba(
      103,
      48,
      220,
      0.2
    );
}

.continue-copy {
  min-width:
    0;

  flex: 1;
}

.continue-copy h2,
.continue-card.empty h2 {
  margin:
    2px 0 5px;

  font-size:
    19px;
}

.continue-copy p,
.continue-card.empty p {
  margin: 0;

  color:
    var(
      --learn-muted
    );
}

.continue-progress {
  display:
    grid;

  grid-template-columns:
    minmax(
      0,
      1fr
    )
    auto;

  align-items:
    center;

  gap: 12px;

  margin-top:
    22px;
}

.continue-progress strong {
  color:
    var(
      --learn-purple
    );
}

.continue-button {
  float: right;

  margin-top:
    26px;
}

.continue-card.empty {
  display:
    flex;

  flex-direction:
    column;

  align-items:
    flex-start;
}

.continue-card.empty
.continue-empty-icon {
  margin-bottom:
    16px;
}

.continue-card.empty
.primary-button {
  margin-top:
    auto;
}

.subjects-section {
  min-width: 0;
}

.section-title-row {
  display:
    flex;

  align-items:
    center;

  justify-content:
    space-between;

  gap: 15px;

  margin:
    4px 0 14px;
}

.section-title-row h2 {
  display:
    flex;

  align-items:
    center;

  gap: 9px;

  margin: 0;

  font-size:
    18px;
}

.text-link {
  display:
    inline-flex;

  align-items:
    center;

  color:
    #07955a;

  text-decoration:
    none;

  font-size:
    13px;

  font-weight:
    850;
}

.subject-scroller {
  display:
    grid;

  grid-auto-flow:
    column;

  grid-auto-columns:
    minmax(
      145px,
      1fr
    );

  gap: 12px;

  overflow-x:
    auto;

  padding:
    2px 2px 10px;

  scroll-snap-type:
    x mandatory;

  scrollbar-width:
    thin;
}

.subject-card {
  min-height:
    285px;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  padding:
    17px 13px;

  background:
    #ffffff;

  border:
    1px solid
    currentColor;

  border-radius:
    16px;

  scroll-snap-align:
    start;
}

.subject-icon {
  width:
    67px;

  height:
    67px;

  display:
    grid;

  place-items:
    center;

  margin-bottom:
    15px;

  color:
    #ffffff;

  background:
    currentColor;

  border-radius:
    50%;

  font-size:
    25px;

  font-weight:
    900;
}

.subject-card h3 {
  margin:
    0 0 9px;

  color:
    var(
      --learn-navy
    );

  text-align:
    center;

  font-size:
    14px;
}

.subject-card > p {
  margin: 0;

  color:
    var(
      --learn-muted
    );

  font-size:
    12px;
}

.subject-progress {
  width:
    100%;

  margin-top:
    20px;
}

.subject-card
.progress-fill {
  background:
    currentColor;
}

.subject-status {
  margin-top:
    11px;

  color:
    var(
      --learn-muted
    );

  text-align:
    center;

  font-size:
    11px;
}

.subject-button {
  min-height:
    38px;

  width:
    100%;

  display:
    inline-flex;

  align-items:
    center;

  justify-content:
    center;

  gap: 3px;

  margin-top:
    auto;

  padding:
    8px;

  color:
    currentColor;

  text-decoration:
    none;

  background:
    rgba(
      255,
      255,
      255,
      0.7
    );

  border:
    1px solid
    currentColor;

  border-radius:
    9px;

  font-size:
    11px;

  font-weight:
    850;
}

.subject-blue {
  color:
    #3f9df2;

  background:
    #f7fbff;
}

.subject-green {
  color:
    #20b86c;

  background:
    #f5fcf8;
}

.subject-orange {
  color:
    #ff9d16;

  background:
    #fffaf3;
}

.subject-pink {
  color:
    #ed4ead;

  background:
    #fff7fc;
}

.subject-purple {
  color:
    #8052eb;

  background:
    #faf8ff;
}

.subject-cyan {
  color:
    #17abc0;

  background:
    #f4fdff;
}

.subject-yellow {
  color:
    #d9a100;

  background:
    #fffcf0;
}

.subjects-empty {
  min-height:
    220px;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  justify-content:
    center;

  padding:
    25px;

  text-align:
    center;

  background:
    #ffffff;

  border:
    1px dashed
    #d7deea;

  border-radius:
    17px;
}

.subjects-empty svg {
  color:
    var(
      --learn-green
    );
}

.subjects-empty h3 {
  margin:
    13px 0 6px;
}

.subjects-empty p {
  max-width:
    350px;

  margin: 0;

  color:
    var(
      --learn-muted
    );
}

.animated-banner {
  position:
    relative;

  min-height:
    150px;

  display:
    grid;

  grid-template-columns:
    130px
    minmax(
      0,
      1fr
    )
    auto
    110px;

  align-items:
    center;

  gap: 20px;

  margin-top:
    25px;

  padding:
    22px 30px;

  overflow:
    hidden;

  background:
    linear-gradient(
      135deg,
      #eafbf6,
      #f4fcff
    );

  border:
    1px solid
    #d4f0e7;

  border-radius:
    18px;
}

.animated-illustration,
.popcorn-art {
  display:
    grid;

  place-items:
    center;

  font-size:
    74px;
}

.animated-copy h2 {
  margin:
    0 0 8px;

  text-transform:
    uppercase;

  font-size:
    21px;
}

.animated-copy p {
  margin: 0;

  color:
    var(
      --learn-muted
    );
}

.completed-learning {
  margin-top:
    8px;
}

.completed-intro {
  display:
    flex;

  align-items:
    center;

  gap: 15px;

  margin-bottom:
    18px;

  padding:
    18px 20px;

  background:
    linear-gradient(
      135deg,
      #fff,
      #f5f2ff
    );

  border:
    1px solid
    #e7ddff;

  border-radius:
    16px;
}

.completed-intro-icon {
  width:
    52px;

  height:
    52px;

  flex:
    0 0 52px;

  display:
    grid;

  place-items:
    center;

  color:
    #ffffff;

  background:
    var(
      --learn-purple
    );

  border-radius:
    14px;
}

.completed-intro h2 {
  margin:
    0 0 5px;

  font-size:
    19px;
}

.completed-intro p {
  margin: 0;

  color:
    var(
      --learn-muted
    );
}

.completed-course-grid {
  display:
    grid;

  grid-template-columns:
    repeat(
      auto-fit,
      minmax(
        300px,
        1fr
      )
    );

  gap: 18px;
}

.completed-course-card {
  overflow:
    hidden;

  background:
    #ffffff;

  border:
    1px solid
    var(
      --learn-border
    );

  border-radius:
    18px;

  box-shadow:
    var(
      --learn-shadow
    );

  transition:
    transform
    180ms ease,
    box-shadow
    180ms ease;
}

.completed-course-card:hover {
  transform:
    translateY(
      -2px
    );

  box-shadow:
    0 13px 32px
    rgba(
      25,
      55,
      95,
      0.11
    );
}

.completed-course-cover {
  position:
    relative;

  height:
    155px;

  overflow:
    hidden;

  background:
    linear-gradient(
      135deg,
      #173d82,
      #5f38ce
    );
}

.completed-course-cover
img {
  width:
    100%;

  height:
    100%;

  object-fit:
    cover;
}

.course-cover-fallback {
  width:
    100%;

  height:
    100%;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  justify-content:
    center;

  gap: 8px;

  color:
    #ffffff;
}

.course-cover-fallback
span {
  font-weight:
    850;
}

.completed-badge {
  position:
    absolute;

  top: 13px;

  right: 13px;

  display:
    inline-flex;

  align-items:
    center;

  gap: 5px;

  padding:
    6px 10px;

  color:
    #087d4d;

  background:
    rgba(
      255,
      255,
      255,
      0.93
    );

  border-radius:
    999px;

  font-size:
    11px;

  font-weight:
    850;
}

.completed-course-body {
  padding:
    19px;
}

.completed-course-meta {
  display:
    flex;

  flex-wrap:
    wrap;

  gap: 7px;
}

.completed-course-meta
span {
  padding:
    5px 8px;

  color:
    #46607f;

  background:
    #edf3fa;

  border-radius:
    999px;

  font-size:
    10px;

  font-weight:
    800;
}

.completed-course-body
h3 {
  margin:
    13px 0 8px;

  font-size:
    19px;
}

.completion-date {
  display:
    flex;

  align-items:
    center;

  gap: 7px;

  margin:
    0 0 17px;

  color:
    var(
      --learn-muted
    );

  font-size:
    12px;
}

.course-resource-grid {
  display:
    grid;

  grid-template-columns:
    repeat(
      3,
      1fr
    );

  gap: 8px;

  margin-bottom:
    17px;
}

.course-resource-grid
> div {
  min-width: 0;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  padding:
    11px 5px;

  color:
    var(
      --learn-purple
    );

  background:
    #f7f4ff;

  border-radius:
    11px;
}

.course-resource-grid
strong {
  margin-top:
    4px;

  color:
    var(
      --learn-navy
    );

  font-size:
    14px;
}

.course-resource-grid
span {
  color:
    var(
      --learn-muted
    );

  font-size:
    10px;
}

.completed-course-button {
  min-height:
    43px;

  width:
    100%;

  display:
    flex;

  align-items:
    center;

  justify-content:
    center;

  gap: 6px;

  color:
    #ffffff;

  text-decoration:
    none;

  background:
    linear-gradient(
      135deg,
      #7440e8,
      #5828c8
    );

  border-radius:
    10px;

  font-size:
    12px;

  font-weight:
    850;
}

.completed-empty {
  min-height:
    390px;

  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  justify-content:
    center;

  padding:
    35px;

  text-align:
    center;

  background:
    #ffffff;

  border:
    1px dashed
    #d9dfeb;

  border-radius:
    18px;
}

.completed-empty-icon {
  width:
    80px;

  height:
    80px;

  display:
    grid;

  place-items:
    center;

  color:
    var(
      --learn-purple
    );

  background:
    var(
      --learn-purple-soft
    );

  border-radius:
    50%;
}

.completed-empty h2 {
  margin:
    18px 0 8px;
}

.completed-empty p {
  max-width:
    520px;

  margin:
    0 0 20px;

  color:
    var(
      --learn-muted
    );
}

.ask-mentor-button {
  position:
    fixed;

  z-index:
    90;

  right:
    24px;

  bottom:
    24px;

  min-height:
    48px;

  display:
    inline-flex;

  align-items:
    center;

  gap: 9px;

  padding:
    11px 18px;

  color:
    #ffffff;

  text-decoration:
    none;

  background:
    #082954;

  border-radius:
    999px;

  box-shadow:
    0 10px 25px
    rgba(
      5,
      31,
      68,
      0.2
    );

  font-size:
    13px;

  font-weight:
    850;
}

.student-bottom-nav {
  display:
    none;
}

.learn-state-page {
  min-height:
    100dvh;

  padding:
    40px;
}

.learn-state-page.centered {
  display:
    flex;

  flex-direction:
    column;

  align-items:
    center;

  justify-content:
    center;

  text-align:
    center;
}

.learn-state-page.centered
p {
  color:
    var(
      --learn-muted
    );
}

.skeleton {
  overflow:
    hidden;

  background:
    linear-gradient(
      90deg,
      #e9edf3,
      #f7f9fc,
      #e9edf3
    );

  background-size:
    200% 100%;

  border-radius:
    16px;

  animation:
    skeletonMove
    1.5s
    linear
    infinite;
}

.skeleton.heading {
  width:
    280px;

  height:
    55px;
}

.skeleton.search {
  width:
    100%;

  height:
    52px;

  margin-top:
    25px;
}

.skeleton.program {
  width:
    100%;

  height:
    230px;

  margin-top:
    18px;
}

.skeleton.cards {
  width:
    100%;

  height:
    300px;

  margin-top:
    18px;
}

@keyframes
headerFloat {
  0%,
  100% {
    transform:
      translateY(
        0
      );
  }

  50% {
    transform:
      translateY(
        -5px
      );
  }
}

@keyframes
rocketFloat {
  0%,
  100% {
    transform:
      translateY(
        0
      )
      rotate(
        8deg
      );
  }

  50% {
    transform:
      translateY(
        -6px
      )
      rotate(
        12deg
      );
  }
}

@keyframes
skeletonMove {
  from {
    background-position:
      200% 0;
  }

  to {
    background-position:
      -200% 0;
  }
}

@media (
  max-width:
    1050px
) {
  .ignite-program {
    grid-template-columns:
      minmax(
        0,
        1fr
      )
      minmax(
        230px,
        0.5fr
      );
  }

  .ignite-rocket-art {
    display:
      none;
  }

  .learning-main-grid {
    grid-template-columns:
      1fr;
  }

  .continue-card {
    min-height:
      230px;
  }
}

@media (
  max-width:
    767px
) {
  .learn-page {
    padding-bottom:
      88px;
  }

  .student-topbar {
    position:
      sticky;

    z-index:
      80;

    top: 0;

    min-height:
      72px;

    display:
      flex;

    align-items:
      center;

    justify-content:
      space-between;

    padding:
      11px 18px;

    background:
      rgba(
        255,
        255,
        255,
        0.96
      );

    border-bottom:
      1px solid
      var(
        --learn-border
      );

    backdrop-filter:
      blur(
        15px
      );
  }

  .mobile-logo
  img {
    width:
      112px;

    max-height:
      45px;

    object-fit:
      contain;
  }

  .topbar-actions {
    display:
      flex;

    align-items:
      center;

    gap: 10px;
  }

  .icon-button {
    position:
      relative;

    width:
      43px;

    height:
      43px;

    display:
      grid;

    place-items:
      center;

    color:
      var(
        --learn-navy
      );

    background:
      #ffffff;

    border:
      1px solid
      var(
        --learn-border
      );

    border-radius:
      50%;
  }

  .notification-dot {
    position:
      absolute;

    top: 7px;

    right: 7px;

    width:
      8px;

    height:
      8px;

    background:
      #ff4259;

    border:
      2px solid
      #ffffff;

    border-radius:
      50%;
  }

  .mobile-profile {
    width:
      43px;

    height:
      43px;

    display:
      grid;

    place-items:
      center;

    overflow:
      hidden;

    color:
      #ffffff;

    text-decoration:
      none;

    background:
      var(
        --learn-green
      );

    border-radius:
      50%;

    font-weight:
      900;
  }

  .mobile-profile
  img {
    width:
      100%;

    height:
      100%;

    object-fit:
      cover;
  }

  .learn-content {
    width:
      calc(
        100% - 28px
      );

    padding:
      21px 0
      30px;
  }

  .learn-header {
    min-height:
      auto;

    padding:
      0;

    overflow:
      visible;

    background:
      transparent;

    border: 0;

    border-radius:
      0;
  }

  .learn-header h1 {
    font-size:
      31px;
  }

  .learn-header p {
    font-size:
      14px;
  }

  .learn-header-art {
    display:
      none;
  }

  .learning-tabs {
    width:
      100%;

    display:
      grid;

    grid-template-columns:
      1fr
      1fr;

    margin-top:
      20px;
  }

  .learning-tab {
    min-width:
      0;

    padding:
      9px 8px;

    font-size:
      12px;
  }

  .learn-search-row {
    grid-template-columns:
      minmax(
        0,
        1fr
      )
      auto;

    gap: 8px;
  }

  .learn-search {
    min-width:
      0;

    padding:
      0 13px;
  }

  .learn-search input {
    font-size:
      12px;
  }

  .subject-filter {
    min-width:
      122px;

    padding:
      0 12px;
  }

  .subject-filter select {
    font-size:
      12px;
  }

  .ignite-program {
    grid-template-columns:
      1fr;

    gap: 16px;

    padding:
      20px;

    border-left:
      1px solid
      rgba(
        18,
        173,
        105,
        0.25
      );
  }

  .ignite-main {
    min-width:
      0;
  }

  .ignite-title-row {
    align-items:
      flex-start;
  }

  .ignite-icon {
    font-size:
      35px;
  }

  .title-with-badge
  h2 {
    font-size:
      20px;
  }

  .ignite-days {
    margin-right:
      -20px;

    padding-right:
      20px;
  }

  .ignite-day {
    min-width:
      64px;

    padding:
      7px 10px;
  }

  .next-session {
    padding:
      15px;
  }

  .next-session p {
    min-height:
      auto;
  }

  .next-session
  .primary-button {
    width:
      100%;
  }

  .learning-main-grid {
    gap: 22px;
  }

  .continue-card {
    min-height:
      245px;

    padding:
      20px;
  }

  .continue-button {
    width:
      100%;

    float:
      none;
  }

  .subject-scroller {
    grid-auto-columns:
      minmax(
        155px,
        44vw
      );

    margin-right:
      -14px;

    padding-right:
      14px;
  }

  .subject-card {
    min-height:
      285px;
  }

  .animated-banner {
    grid-template-columns:
      72px
      minmax(
        0,
        1fr
      );

    gap: 14px;

    padding:
      21px;

    text-align:
      left;
  }

  .animated-illustration {
    font-size:
      55px;
  }

  .animated-copy
  h2 {
    font-size:
      17px;
  }

  .animated-copy
  p {
    font-size:
      12px;
  }

  .animated-banner
  .primary-button {
    grid-column:
      1 / -1;

    width:
      100%;
  }

  .popcorn-art {
    display:
      none;
  }

  .completed-intro {
    align-items:
      flex-start;
  }

  .completed-intro
  p {
    font-size:
      12px;
  }

  .completed-course-grid {
    grid-template-columns:
      1fr;
  }

  .ask-mentor-button {
    right:
      16px;

    bottom:
      88px;

    width:
      51px;

    height:
      51px;

    justify-content:
      center;

    padding: 0;
  }

  .ask-mentor-button
  span {
    display:
      none;
  }

  .student-bottom-nav {
    position:
      fixed;

    z-index:
      100;

    right: 0;
    bottom: 0;
    left: 0;

    min-height:
      72px;

    display:
      grid;

    grid-template-columns:
      repeat(
        5,
        minmax(
          0,
          1fr
        )
      );

    padding:
      7px 5px
      calc(
        7px +
        env(
          safe-area-inset-bottom
        )
      );

    background:
      rgba(
        255,
        255,
        255,
        0.97
      );

    border-top:
      1px solid
      var(
        --learn-border
      );

    box-shadow:
      0 -8px
      25px
      rgba(
        25,
        55,
        95,
        0.08
      );

    backdrop-filter:
      blur(
        17px
      );
  }

  .bottom-nav-item {
    min-width:
      0;

    display:
      flex;

    flex-direction:
      column;

    align-items:
      center;

    justify-content:
      center;

    gap: 4px;

    color:
      #818ca0;

    text-decoration:
      none;

    font-size:
      10px;

    font-weight:
      800;
  }

  .bottom-nav-icon {
    width:
      34px;

    height:
      31px;

    display:
      grid;

    place-items:
      center;

    border-radius:
      10px;
  }

  .bottom-nav-item
  .active {
    color:
      var(
        --learn-green
      );
  }

  .bottom-nav-item.active {
    color:
      var(
        --learn-green
      );
  }

  .bottom-nav-item.active
  .bottom-nav-icon {
    background:
      var(
        --learn-green-soft
      );
  }
}

@media (
  max-width:
    480px
) {
  .learn-search-row {
    grid-template-columns:
      1fr;
  }

  .subject-filter {
    width:
      100%;
  }

  .learning-tab
  svg {
    display:
      none;
  }

  .learning-tab {
    font-size:
      11px;
  }

  .ignite-program {
    padding:
      17px;
  }

  .ignite-title-row {
    gap: 9px;
  }

  .ignite-icon {
    font-size:
      29px;
  }

  .title-with-badge
  h2 {
    font-size:
      18px;
  }

  .grade-badge {
    padding:
      4px 7px;

    font-size:
      10px;
  }

  .course-resource-grid {
    gap: 5px;
  }
}

@media (
  prefers-reduced-motion:
  reduce
) {
  *,
  *::before,
  *::after {
    scroll-behavior:
      auto !important;

    animation-duration:
      0.01ms
      !important;

    animation-iteration-count:
      1
      !important;

    transition-duration:
      0.01ms
      !important;
  }
}