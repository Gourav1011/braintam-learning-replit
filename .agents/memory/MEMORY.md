- [Homework MCQ type detection](homework-mcq-type.md) — DB default 'writing' vs questionsJson presence; always detect MCQ from questionsJson, not homework_type column.
- [IST datetime handling](ist-datetime.md) — India-only app: always append ":00+05:30" when parsing datetime-local inputs; use UTC arithmetic for edit pre-fill; add timeZone:"Asia/Kolkata" to display toLocaleString calls.
- [Points recompute wipes non-activity pts](points-recompute.md) — recomputeAndSavePoints must include pointsLedger SUM + dailyCoinClaims SUM or login/streak/coin points vanish every time a student submits work.
- [Mentor role variants require middleware update](mentor-role-variants.md) — adding sales_mentor/academic_mentor roles requires updating requireRole() in ALL route files that gate on "mentor".
- [LiveKit live-class integration patterns](livekit-livecless-integration.md) — role via server-signed token metadata, backend-authoritative stage timer, poll_analytics doubles as leaderboard + permanent history source.
- [LiveKit teacher video production bugs](livekit-teacher-video-bugs.md) — 3 root causes: super_admin excluded from isStaff, dual getUserMedia competing, teacherVideoRef null race.
- [Annotation canvas must always render](annotation-canvas.md) — canvas must be outside the embedUrl conditional; placing it inside means pen/highlight/clear have no DOM target when no presentation is loaded.
- [Live classroom sidebar architecture](live-classroom-sidebars.md) — TeacherSidebar (LIVE only, no tabs, purple for suggested), MentorSidebar (group stats + call absent); mentor:suggestStudent → teacher:studentSuggested in teacherRoom only; staffChat via teacherRoom.
- [Unmemoized callback prop causes infinite refresh](unmemoized-callback-prop-rerender.md) — a plain-function prop (e.g. `flash`) recreated each render breaks child `useCallback([load],[flash])` → useEffect refetch loops.
- [DATABASE_URL vs NEON_DATABASE_URL divergence](neon-vs-local-staff-accounts.md) — always query NEON_DATABASE_URL for staff/login debugging; local DB rows don't reflect production.
- [Program-based teacher assignment model](teacher-course-subject-assignment.md) — teachers assign via Program→Course→Subject (nullable courseSubjectId = whole course), not grade-locked like mentors; inactive teachers must be blocked from new assignments and live-class scheduling.
- [Braintam admin has two distinct Teacher UIs](braintam-dual-teacher-pages.md) — "Teacher Management" (Command Center→Administration, command-center-teachers-tab.tsx) and "Teachers" (Command Center→Analytics, teacher-analytics-tab.tsx via index.tsx) are separate pages; confirm which one the user is actually viewing before editing.
- [Express route param ordering](express-route-param-ordering.md) — static sub-paths must be registered before /:id on same base path; restart server after reorder, stale process can mask the fix.
- [Mentor live-classes tab must merge two sources](mentor-live-classes-merge.md) — demo_sessions and course-based live_classes are independent systems; mentor query must resolve+merge both via student enrollments.
- [innerJoin silently drops nullable-FK rows](inner-join-nullable-fk.md) — courses without a direct subjectId (mastery courses using course_subjects instead) vanished from teacher's course list because query used innerJoin on subjects; use leftJoin whenever the FK column is nullable.
- [DemoPaywall blocked all content](demo-paywall-modal.md) — the old paywall blurred/blocked the entire page for demo students; redesigned as a dismissible auto-popup so real content (courses etc.) is always visible.
- [LiveKit "teacher" role check too narrow](livekit-teacher-role-broadening.md) — instructor video/audio only rendered for role==="teacher"; admins/super_admins assigned as live instructors were invisible. Treat teacher/admin/super_admin as the broadcaster role.
- [Live class status must auto-sync, not rely on manual clicks](live-class-status-autosync.md) — status only changed via teacher PATCH; added a 30s backend job deriving live/completed from scheduledAt+duration so all portals agree automatically.
- [Live classroom identity must come from auth, not URL params](live-classroom-identity-source.md) — no nav link ever passed name/userId query params to /live/:id, so every visitor fell back to generic "Student"/"u-student", breaking chat names and presence.
- [Chat groupId branch must broadcast, not echo](chat-groupid-echo-bug.md) — course-based live classes have no groupId; the no-groupId branch in chat:send only emitted back to the sender's own socket, so nobody else ever saw the message.
- [LiveKit room isolation and connection decoupling](livekit-room-isolation.md) — deterministic `braintam-live-<id>` room per class (isNull-guarded write); LiveKit connection state must never be gated on Socket.IO's connected state.
- [Live classroom role must come from useAuth, not URL](live-classroom-role-source.md) — role was read from ?role= URL param; teachers/admins navigating without that param got "student" role and saw student UI. Fix: use authRole from useAuth() first.
- [Presentation sync must use Socket.IO, not local state](presentation-sync-socketio.md) — presentationUrl was pure local React state; students never received teacher's slides. Fix: emit presentation:start/stop on teacher actions; roomState includes activePresentation for late joiners.
- [Stage video invisible on late join — remoteParticipants seeding](stage-video-late-join.md) — use-livekit only seeded teacher tracks on connect; non-teacher (stage) publishers already streaming were missing from stagePublishers. Fix: iterate ALL remoteParticipants on connect.
- [Stage timer restarts on reconnect — seed with remaining time](stage-timer-remaining-time.md) — loadStageSlots loaded ALL slots (including ended); seeding block used fresh 60s timer. Fix: filter status="active", use stageExpiresAt-Date.now() as delay, idempotency guard on endStageSlot.
- [DEV flag alone hides diagnostics from real testers](diagnostics-dev-flag.md) — import.meta.env.DEV is true on Replit preview which real testers use. Require ?debug=classroom to show diagnostics panel.
- [Additive schema safeguards](additive-schema-safeguards.md) — protect fresh deployments when full schema push is blocked by unrelated database drift.
- [Workplace membership revocation](workplace-membership-revocation.md) — revoke membership and scoped notifications before evicting/disconnecting every affected Workplace socket.
